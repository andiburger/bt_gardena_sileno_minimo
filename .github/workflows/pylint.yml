name: Pylint

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # Ich habe Python 3.11 und 3.12 ergänzt, da dein Dockerfile 3.12 nutzt!
        python-version: ["3.10", "3.11", "3.12"] 
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5 # Update auf v5 (neueste Version)
      with:
        python-version: ${{ matrix.python-version }}
        cache: 'pip' # Beschleunigt den Workflow erheblich!
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pylint
        # WICHTIG: Installiere die Pakete deines Projekts!
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
        
    - name: Analysing the code with pylint
      run: |
        # --errors-only sorgt dafür, dass der Workflow nur bei kritischen Bugs 
        # fehlschlägt, nicht aber bei Stil-Fragen (wie zu kurzen Variablennamen).
        pylint --errors-only $(git ls-files '*.py')
